#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes a char to stdout
 * @a: The character to print
 *
 * Return: success 1.
 * error, -1 is returned
 */
int _putchar(char a)
{
	return (write(1, &a 1));
}
